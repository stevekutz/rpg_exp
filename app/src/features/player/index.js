import React from 'react';

export const Player = (props) => {
    return (
        <div>
            Player
        </div>
    )
}

export const Title = (props) => {
    return (
        <div>
            Title
        </div>
    )
}